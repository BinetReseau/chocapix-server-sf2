<?php
namespace BR\BarBundle\Type\{{namespace}};

use Symfony\Component\Form\AbstractType;
use Symfony\Component\Form\FormBuilderInterface;
use Symfony\Component\OptionsResolver\OptionsResolverInterface;

class {{class}}Type extends AbstractType
{
    public function buildForm(FormBuilderInterface $builder, array $options) {
        $builder
{% for p in properties %}
{% if p.isEntity %}
            ->add('{{p.name}}', '{{p.entityName}}_id', array('property_path' => '{{p.entityName}}'))
            ->add('{{p.entityName}}', '{{p.entityName}}', array('read_only' => true))
{% else %}
            ->add('{{p.name}}')
{% endif %}
{% endfor %}
            ->add('_type', 'text', array('data' => '{{namespace}}\{{class}}', 'mapped' => false))
            ;
    }

    public function setDefaultOptions(OptionsResolverInterface $resolver)
    {
        $resolver->setDefaults(array(
             'data_class' => '{{fullClassName}}',
        ));
    }

    public function getName() {
        return '{{shortName}}';
    }
}
